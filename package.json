{
  "name": "loop",
  "version": "1.4.3",
  "description": "",
  "keywords": [
    "school",
    "trade",
    "marketplace"
  ],
  "homepage": "https://github.com/ezemastro/loop#readme",
  "bugs": {
    "url": "https://github.com/ezemastro/loop/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/ezemastro/loop.git"
  },
  "license": "Apache-2.0",
  "author": "Ezequiel Mastropietro",
  "type": "commonjs",
  "main": "",
  "scripts": {
    "lint": "npm run lint --prefix server/api && npm run lint --prefix client && npm run lint --prefix adminClient",
    "lint:fix": "npm run lint --prefix server/api -- --fix && npm run lint --prefix client -- --fix && npm run lint --prefix adminClient -- --fix",
    "format": "npx prettier --write \"**/*.{js,ts,tsx,mts,cts,json,css,scss,md}\"",
    "format:check": "npx prettier --check \"**/*.{js,ts,tsx,mts,cts,json,css,scss,md}\"",
    "build:server": "docker build -f api.Dockerfile -t ezemastro/loop:$npm_package_version -t ezemastro/loop:latest .",
    "docker:build": "node scripts/docker-build.js",
    "docker:build:patch": "node scripts/docker-build.js patch",
    "docker:build:minor": "node scripts/docker-build.js minor",
    "docker:build:major": "node scripts/docker-build.js major",
    "docker:push": "node scripts/docker-push.js",
    "start": "docker compose -f docker-compose.prod.yml up",
    "docker:deploy": "docker compose up -d --pull always",
    "dev:old": "concurrently -n api,client,admin -c green,blue,yellow \"cd server && docker compose up -d && docker compose watch api\" \"npm start --prefix client\" \"npm run dev --prefix adminClient\"",
    "dev": "node scripts/dev.js",
    "dev:migrate": "docker compose -f docker-compose.dev.yml exec api npm run migrate",
    "dev:seed": "docker compose -f docker-compose.dev.yml exec api npm run seed",
    "dev:seed:demo": "docker compose -f docker-compose.dev.yml exec api npm run seed -- --demo",
    "test:e2e": "bash scripts/run-e2e.sh",
    "test:e2e:down": "docker compose -p loop-e2e -f docker-compose.e2e.yml down -v --remove-orphans"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "eslint": "^9.33.0",
    "prettier": "^3.6.2"
  }
}
