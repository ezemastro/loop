// Punto de entrada para los componentes. La implementación vive en el store para que `vars()` y
// los valores JS lean exactamente la misma paleta.
export { useActiveTheme as useThemeColors } from "@/stores/theme";
